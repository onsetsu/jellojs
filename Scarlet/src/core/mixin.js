/**
 * Extend Scarlet with a convenient function for enhance objects with mixed in behaviour objects.
 * 
 */
Scarlet.Mixin = function(receiver, holder)
{
	// so in etwa
	return a;
};

/* oder: 
p({}).mixin(
	p.Observee()
).subscribe(
	p.Observer({
		refresh: function(){}
	})
);
*/